# CHIP-8 Emulator in Rust 🎮

A tiny and fun Chip-8 emulator written in Rust.

```bash
# Play tetris
cargo run

# Play other ROM from file in /rom
cargo run -- --UFO
```

This project teaches you about bitwise operations and how CPU's work. I recommend every does this!
